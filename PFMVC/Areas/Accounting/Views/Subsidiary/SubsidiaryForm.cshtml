@model DLL.ViewModel.VM_acc_Subsidiary

@{
    Layout = null;
}

<fieldset>
    <legend id="addGroupLegend">Add Subsidiary Name@*<button class="btn btn-default btn-xs pull-right" onclick="getGroupTree()" id="report">Group Tree</button> <button class="btn btn-default btn-xs pull-right" onclick="getTreeView()" id="tvw"><span class="glyphicon glyphicon-tree-conifer"></span></button>*@</legend>

@using (Ajax.BeginForm("SubsidiaryForm", "Subsidiary", null, new AjaxOptions
{
    InsertionMode = InsertionMode.Replace,
    HttpMethod = "POST",
    OnSuccess = "updateSuccess"
}, new { @id = "DialogForm1" }))
    {
        @Html.AntiForgeryToken()
        <div id="update-message1"></div>
       
        <table class="table table-striped">          

              <tr>
                <td class="a">@Html.Label("New Subsidiary Name")</td>
                <td>@Html.TextBoxFor(m => m.Subsidiary_Name, new { @class = "form-control next", @id="Subsidiary_Name", placeholder="New Subsidiary Name..." }) @Html.ValidationMessageFor(m => m.Subsidiary_Name)</td>
                  <td class="hidden">@Html.TextBoxFor(m => m.Subsidiary_Id)</td>
            </tr>          
          
            
        </table>
    <label style="color:lightsteelblue; font-style:italic">Press ENTER to move on NEXT</label>
        <hr />@*
        if (Model != null)
        {
            <button class="btn btn-danger" type="button"  onclick="deleteGroup(@Model.GroupID);">
                <span class="glyphicon glyphicon-trash"></span>
            </button>
            <button class="btn btn-primary" type="button" onclick="NewGroup();" style="padding-left: 10px">
                <span class="glyphicon glyphicon-plus"></span>
            </button>
        }
        *@
        <button class="btn btn-success pull-right 3 next" type="button" onclick="save();" id="save_btn">
            <span class="glyphicon glyphicon-saved"></span> Save
        </button>
    }
     @if (Model != null)
     {
        <br />
        <hr />
        <label class="mini">@ViewBag.User</label>
    }
</fieldset>

<style>
    .a {
        vertical-align:middle !important;
        max-width:80px;
    }

</style>

<script>
    $('#Subsidiary_Name').focus();
</script>
